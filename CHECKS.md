# nofs4 检查记录

## 修改范围

与发布包 nofs3 逐字节比较：

- `app.js` 除版本常量外完全一致；未增加测试接口到发布代码。
- `index.html` 除版本文字和查询参数外完全一致。
- `styles.css` 只替换 3 个已有选择器块：计时框、节拍容器、节拍实心圆，没有追加另一套覆盖规则。
- `manifest.webmanifest`、`sw.js`、SVG 与 3 张 PNG 图标逐字节一致。
- `node --check app.js` 通过。

## 布局检查

在本地 Chromium 144 的离线页面测试中，比较修改前后的布局。测试使用内存注入的状态接口，该接口没有加入 ZIP 发布代码。

9 种视口：320×568、360×640、390×780、430×932、568×320、667×375、844×390、932×430、1024×768。

每种视口分别检查日语、中文和英文，控制栏显示／隐藏两种状态，共 54 组组合。

- 计时框、节拍对齐容器、语言框、问号和录制按钮的位置及外部尺寸与 nofs3 相同（容差 0.55 CSS px）。
- 计时框的左边线、宽高、圆角继续与语言框一致。
- 16 px 实心圆的圆心仍与右侧问号／录制按钮同轴。
- 节拍外容器背景为完全透明，边框为 0；圆点本身没有轮廓线。
- 计时文字在各尺寸及 `25:01:01` 小时格式下没有水平溢出。
- 控制栏隐藏前后，两侧状态显示均在可见区域内。
- 状态层不拦截点击，命中测试仍落到 canvas。

## 毛玻璃检查

计时框的计算背景颜色与控制栏相同，标准 `backdrop-filter` 均为 `blur(10px)`。

在带细条纹的测试图片上，将实际截屏与临时关闭模糊后的截屏比较：计时框内部横向边缘变化均值从 5.754 降至 0.073，确认背景确实被模糊，而非只有 CSS 声明。

## 实际交互与录制

通过真实按钮触发照片导入、播放／停止、状态层显隐、录制、保存弹窗及下载。非录制演奏只显示节拍圆；录制时显示计时；停止后隐藏状态层。检查红／白／白／白的颜色序列。

录制中改变视口方向，计时起点和输出画布尺寸保持不变。下载样本为 MP4，约 2.5102 秒，960×1920，包含音频和视频轨道。此尺寸来自测试竖图的原比例，不是新增的视频规格。

测试没有未捕获脚本异常；全屏调用次数为 0。

## 限制

上述是本地 Chromium 的移动尺寸模拟，不代表实体 iPhone Safari／Android Chrome 的实机结果。`-webkit-backdrop-filter` 声明已加入，但本次未在 WebKit 浏览器执行视觉验证。测试主要覆盖本次样式修改；不声称应用没有其他既有问题。
