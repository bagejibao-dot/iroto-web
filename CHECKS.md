# nofs12 检查记录

## 源码范围

基底：本对话 `Iroto_Web_logo_v3_browser_help_nofs11.zip`。

- `node --check app.js` 通过；PostCSS 解析 `styles.css` 和 `ui.css` 通过。
- 比较 133 个既有顶层具名函数：131 个逐字一致；只改 `helpSymbol` 和 `updateHelpDialog` 两个帮助显示函数。
- 除这两个函数、HELP_TEXT 帮助文案对象和版本常量外，整个 app.js 逐字一致。
- HELP_TEXT 中保存/删除窗口文案、录制仍在继续提示和实际校准按钮引用的标题保持不变。
- 三语均为 3 个帮助部分、7 个按键卡片、7 个颜色对照、3 条颜色说明；没有语言/帮助卡片，没有失效的 pack 文案引用。
- ui.css 只替换帮助图标规则：播放/停止组合由文字改为固定尺寸 SVG，删除已不再使用的 type-language 选择器。没有修改弹窗尺寸、内边距、字号、颜色、滚动、关闭/版本位置或文件名编辑框。
- HTML 除 nofs11→nofs12 版本替换外完全一致；无重复 id，实际静态 DOM 引用均存在（helpLiveRecording 为原有动态元素）。
- 逐字节不变的文件：apple-touch-icon.png, icon-192.png, icon-512.png, iroto-logo.svg, manifest.webmanifest, styles.css, sw.js。
- 测试辅助接口、截图、测试脚本及视频样本没有放入发布包。

## 本地布局检查

浏览器：Chromium 144.0.7559.96（Linux 桌面实现）。本地 URL 导航被运行环境限制，因此离线注入 HTML/CSS/JS。启用触摸/移动视口模拟，但不是实体手机或 WebKit。

10 种 CSS 视口 × 日语、中文、英语，共 30 组：320×568、360×640、390×780、430×932、568×320、667×375、844×390、932×430、640×280、1024×768。

- 每组检查正文在顶部和滚动到底时的界限；帮助窗口、X、标题和完整版本号在可见区域内。
- 文字及嵌套卡片无水平溢出，逐文本节点检查文字范围没有越过其容器左右边缘。
- 正文可滚动至底部；标题栏和版本页脚在滚动前后位置一致。
- 播放/停止 SVG 为28×18 px，外圈44×44 px，中心偏差小于0.7 CSS px。小图标不越出圆圈。
- 语言/帮助介绍卡片已删除，但主界面语言选择与问号仍可正常使用；X和Escape均可关闭帮助。
- 已目视查看日语竖屏、英语横屏、中文颜色区截图。没有重新设计旧版的其他图标。

相同视口按实际可见正文字符数比较（不是阅读速度/可用性实验）：

| 语言 | nofs11 | nofs12 | 减少 |
|---|---:|---:|---:|
| 日语 | 1120 | 478 | 57.3% |
| 中文 | 880 | 418 | 52.5% |
| 英语 | 2463 | 1008 | 59.1% |

## 交互与有限录制检查

使用实际 Web Audio 和 MediaRecorder，临时暴露测试状态以检查记录起点；此接口没有写入交付文件。

- 选图、录制待机、开始演奏/录制正常。
- 录制中打开帮助，仍显示原有“録画は続いています。”提示；关闭、改变视口、再次开关帮助，录制起点不重置。
- 停止后保存窗口出现；文件名实际字号仍为16px，编辑/退格不触发返回首页。
- 删除二次确认可取消，取消后录制和名称保留。
- 实际下载 `Iroto_nofs12_test.mp4`，65086 字节；ffprobe 检查为1440×1080、VP9视频与Opus音频轨道、约1.219秒。短样本仅证明本次录制/保存流程可完成，不证明手机画质或帧率。
- 下载后重新播放与停止正常。
- 本次检查未观察到未捕获的页面脚本异常。

## 未覆盖

无实体 iPhone/iPad Safari、Android手机、原生键盘/选字、真实传感器、硬件振动、图库和硬件编码验证。没有重测全部历史场景，不保证所有浏览器无错误，也不宣称本次修复与帮助无关的既有问题。
