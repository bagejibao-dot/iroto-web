# nofs3 本地检查记录

## 源码边界

`node --check app.js` 通过。与 nofs2 对比，共 102 个既有具名函数逐字一致，包括姿态输入处理、坐标矩阵、屏幕旋转同步、准星平滑、强弱拍震动、录制画布、编码和保存函数。

7 个既有函数有显示相关改动：`applyLanguage` 添加新状态的本地化；`draw` 增加视觉脉冲刷新；`triggerTick` 通知当前拍；`setPerformanceUiVisible` 同步提示层；录制计时的三个函数改为更新数字子元素及可见状态。新增 9 个仅用于显示、位置测量及重排的辅助函数。

原 CSS 内容完整保留，新状态层使用独立 ID/类名，避免套用旧 `.record-timer` 的位置覆盖规则。HTML 用一个提示层替代旧计时 DOM，并更新 nofs3 资源标识。测试接口和模拟代码没有放入发布包。

SVG、PNG 桌面图标、manifest、sw.js 逐字节不变。

## 本地 Chromium 检查

主检查 529 项断言、补充检查 57 项断言通过。多数断言是不同视口和语言下重复进行的几何边界检查，不代表已覆盖应用全部功能。

- 320×568、360×640、390×780、430×932、568×320、667×375、844×390、932×430、1024×768 共 9 种视口。
- 每种视口切换日语、中文、英文，分别检查控制栏显示/隐藏后的提示位置。
- 计时胶囊的左边线、宽高、圆角与语言框一致；节拍圆心与问号、底部录制按钮共轴。断言容差小于 0.6 CSS px。
- 顶部栏显示后，计时器/节拍器留有栏下间距；隐藏后上移且都在可视区域以内。
- `25:01:01` 小时格式不溢出同宽胶囊，恢复短格式后样式复原。
- 单点顺序为红、白、白、白，八分音符细分不增加闪光。60、100、160 BPM 的强弱脉冲都在下一拍前结束。
- 实际运行时，视觉四分音符通知与原音频 tick 的时间戳逐项一致。
- 非录制演奏显示节拍、隐藏计时；停止、保存弹窗和返回首页时隐藏提示层。
- 提示层命中测试穿透至照片 canvas，点击可正常切换控制栏。快速显隐后落在最终状态。
- 模拟左右不对称安全边距及顶部安全边距，确认对齐仍使用原控件锚点。
- `prefers-reduced-motion` 下不做平移；缺少 ResizeObserver 时窗口 resize 路径仍能重新对齐。
- 在没有 navigator.vibrate 的 Chromium 模拟环境中，实际录制、旋转、停止和下载通过。
- 录制中旋转保留同一 MediaRecorder、计时起点和画布尺寸。样本文件约 3.02 秒，1440×1080，含视频/音频轨道；编码路径未改。
- 上述测试没有未捕获脚本异常；全屏申请次数为 0。

已目视核对竖屏、横屏、紧凑横屏的控制栏显示/隐藏截图。

## 限制

本地 Chromium 的模拟环境不等于实体 Android 或 iPhone Safari。尚未实测手机硬件振动、实际传感器噪声、原生浏览器栏伸缩、系统键盘及全部操作系统版本。原程序中的其他既有问题不在本次修正范围内。
