# nofs5 检查记录

## 源码差异

`node --check app.js` 通过。按具名函数逐字比较，共识别 118 个函数，其中 114 个与 nofs4 完全一致。仅以下显示函数有改动：

- markVisualBeat：非录制时不更新视觉拍点。
- updateVisualMetronome：非录制时不刷新圆点动画。
- measurePerformanceStatusLayout：计时框改为按顶部栏可见区域居中，低高度横屏紧凑避让。
- syncPerformanceStatus：整个状态层改为录制期间才显示。

除上述函数外，app.js 只有版本常量变化。音频 tick、震动、传感器、旋转校准、计时起点、录制画布及保存函数逐字一致。

HTML 只将节拍圆点移入计时框并移除 REC 文字，同时更新资源查询参数和版本。CSS 只替换已有的状态层区块；其之前的原 UI 样式逐字一致。没有追加多套相互覆盖的状态层样式。DOM ID 唯一，所有资源引用均指向包内存在的文件。

manifest.webmanifest、sw.js、SVG 和 3 张 PNG 图标逐字节一致。测试接口、时钟模拟及屏幕方向模拟代码没有进入发布包。

## 本地布局检查

Chromium 144.0.7559.96，离线载入本地 HTML/CSS/JS。页面导航受到测试环境限制，因此测试文档直接注入浏览器；图片／脚本资源以内联形式加载，不是访问线上站点。仅测试时暴露状态接口，不改变发布代码。

检查 11 种视口：320×568、360×640、390×780、430×932、568×320、667×375、844×390、932×430、1024×768、640×300、640×280。

中日英三种语言 × 控制栏显示／隐藏，共 66 组组合，1038 项布局／状态断言通过。多数断言是相同条件在多个尺寸重复检查，不代表应用所有功能均已覆盖。

检查了居中、框内圆点与数字对齐、长时长 999:01:01、边界／文字溢出、控制栏下方留白、与停止键不重叠、半透明底及 blur(10px)、点击穿透、非录制完全隐藏、快速显隐、左右非对称安全边距、左右横屏、减少动态效果，以及无 ResizeObserver 时的 resize 路径。

## 节拍与实际录制

另有 99 项断言通过。60／100／160 BPM 的 24 个八分拍位置样本验证了红／白／白／白次序、无额外细分闪光、视觉脉冲在下一拍前结束。非录制状态不产生视觉提示。

160 BPM 的原有触觉模式仍为第 1 拍 [60,23,60] ms，其余三拍各 17 ms；本次没有调整。

实际操作按钮完成：选图、普通演奏、停止、录制待机、录制、显示／隐藏控制栏、录制中左右横屏切换、停止、命名窗口、保存下载、再次普通演奏、换图、重新录制、删除确认、返回首页。

录制中使用的是同一 MediaRecorder，计时起点和输出尺寸不变。下载测试样本为 MP4，约 4.282 秒，1440×1080，包含视频和音频轨道。没有调整视频画幅规则。

没有未捕获脚本异常或 Promise 拒绝；全屏请求次数为 0。测试期间模拟缺少 Vibration API，也检查了原振动函数的模式输出。已目视检查实际录制时竖屏／横屏及控制栏显隐截图。

## 限制

以上测试不等于实体 iPhone Safari 或 Android Chrome 的验证；未实测传感器硬件、触觉硬件、移动端系统键盘、真实安全区和浏览器工具栏伸缩。本次不修复与计时框无关的既有功能问题，也不使用“稳定版”作为保证。
