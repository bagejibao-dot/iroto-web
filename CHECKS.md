# nofs6 检查记录

## 源码范围

`node --check app.js` 通过。与 nofs5 逐字比较：app.js 只修改版本常量和 `updateVisualMetronome()`；将该函数和版本号还原后，整个文件与 nofs5 一致。`markVisualBeat()`、`triggerTick()`、振动函数、录制函数、旋转函数及所有其他代码均未改。

CSS 只将 `#beatDot` 原有的半透明灰白底和阴影改为透明、无阴影；未追加覆盖样式。伪元素仍负责绘制红／白圆点。计时框的宽高、内边距、模糊及定位未改。

HTML 只更新资源查询参数及版本文字。manifest、sw.js、SVG、3 张 PNG 均与 nofs5 逐字节一致。测试接口和模拟时钟没有放入发布包。

## 时序检查

提取实际 resetVisualMetronome、markVisualBeat 和 updateVisualMetronome 函数，在受控时钟下检查 60–160 BPM（步长 2）的四拍顺序及亮灭边界；并附加未来更高 BPM 的比例限幅检查、非录制／停止／首拍前检查。共 2304 项重复时序断言通过，并非应用全部功能覆盖。

- 0–280 毫秒范围内保持全亮；超过时长后为零透明度。
- 第一拍红色，另外三拍白色；八分音符细分不重启脉冲。
- 160 BPM 每拍 375 毫秒，亮起 280 毫秒，间隙 95 毫秒。
- 非录制、停止及首拍前无灰色残点。

## 本地 Chromium 检查

Chromium 144.0.7559.96。由于环境限制本地 URL 导航，测试通过直接注入本地 HTML/CSS/JS 的离线文档完成；测试期间临时暴露状态接口用于受控验证，交付文件无测试代码。

6 种视口（320×568、390×780、430×932、568×320、844×390、640×280）× 三语言 × 控制栏显隐，共 36 组组合通过：

- 保持顶部居中和可见区域内；计时文字未溢出。
- 圆点底透明、无残留阴影；亮起为全不透明，间隙为零透明度。
- 圆点亮灭前后，时间文字与计时框的几何边界不变。
- 毛玻璃仍为 blur(10px)。非录制演奏仍隐藏整个提示层。
- 已目视核对红、间隙、白三种状态截图；截图像素对比确认计时文字未移动。

在新页面用真实 Web Audio 时钟和 MediaRecorder，以 160 BPM 完成选图、录制待机、开始录制、停止、命名窗口及下载。录制期间采集了 116 帧状态，观察到四个拍位以及亮／灭两种状态。下载 MP4 为 1440×1080，约 2.344 秒，含视频与音频轨道。此项是录制流程的冒烟检查，不是完整编码兼容性测试。

本次通过的浏览器测试没有未捕获脚本异常或 Promise 拒绝，全屏请求次数为 0。

## 限制

未做实体 iPhone Safari、Android Chrome、手机硬件传感器、振动及真实浏览器工具栏伸缩测试。受控时钟和桌面 Chromium 的结果不保证移动端帧率或重负载情况下的实际显示时间。没有重测 nofs5 的全部历史场景，也没有宣称修复其他既有问题。
